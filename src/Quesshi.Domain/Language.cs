namespace Quesshi.Domain;

/// <remarks>
/// The numbers are persisted and travel over the wire as ints, so they may never be renumbered.
/// </remarks>
public enum Language { Fa = 0, En = 1, Nl = 2 }
