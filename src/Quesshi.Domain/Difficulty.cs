namespace Quesshi.Domain;

/// <summary>
/// Five steps. Three left too much ground between "most people know this" and "genuinely hard",
/// so the ends were doing work they were not suited to.
/// </summary>
public enum Difficulty
{
    VeryEasy = 1,
    Easy = 2,
    Medium = 3,
    Hard = 4,
    VeryHard = 5
}
